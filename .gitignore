# ============================================================================
# STM32F407 + Keil MDK-ARM (ARMCC5) 工程 —— Git 忽略规则
#
# 原则：只跟踪"人写的、丢了无法重建的东西"；
#       凡是编译/链接能重新生成的，以及含机器名或用户名的，一律忽略。
# ============================================================================

# ---------- 1. 编译产物：目标文件 / 依赖 / 交叉引用 ----------
*.o
*.obj
*.d
*.dep
*.crf

# ---------- 2. 链接与输出文件 ----------
*.axf
*.elf
*.hex
*.bin
*.srec
*.map
*.lnp

# 分散加载脚本：Keil 自动生成
# 若日后手写自定义 .sct，请把它移出输出目录并删除下面这行
*.sct

# ---------- 3. 汇编列表 & 链接器报告 ----------
# 注意：Git 的 .gitignore 不支持行内注释，"*.lst # 注释" 会导致整条规则失效，
#       注释必须独占一行（以 # 开头）
# 如 MDK-ARM/port_asm.lst、MDK-ARM/startup_stm32f407xx.lst
*.lst
# 如 MDK-ARM/demo3/demo3.htm（含 CALLGRAPH 静态栈分析）、demo3.build_log.htm
*.htm

# ---------- 4. 调试 / 下载日志 ----------
*.log
*.tra
JLinkLog.txt
JLinkSettings.ini

# ---------- 5. Keil uVision 用户相关文件 ----------
# 窗口布局文件，文件名内嵌用户名（如 demo3.uvguix.ChenShuang），必须忽略
*.uvguix.*
*.uvgui.*

# 注意：以下两项本工程选择【跟踪】，不要忽略：
#   demo3.uvprojx —— 工程结构（源文件清单、编译选项、头文件路径），重建必需
#   demo3.uvoptx  —— 调试器配置与断点，回滚时能一并恢复，故保留
# 若是多人协作且各自调试器不同，再自行忽略 *.uvoptx

# ---------- 6. 备份与临时文件 ----------
*.bak
*.tmp
*~
*.swp

# ---------- 7. 操作系统 ----------
Thumbs.db
desktop.ini
.DS_Store

# ---------- 8. 其他编辑器 / IDE ----------
.vscode/
.idea/
